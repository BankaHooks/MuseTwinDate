diff --git a/bot/bot.py b/bot/bot.py
index 6fff9a4..7383032 100644
--- a/bot/bot.py
+++ b/bot/bot.py
@@ -118,7 +118,7 @@ async def referral_reminder_sender():
                 for user in users:
                     try:
                         if not user.referral_code:
-                            user.referral_code = await crud.generate_referral_code(user.telegram_id)
+                            user.referral_code = await crud.generate_referral_code(session, user.telegram_id)
                             await session.commit()
                         link = f"t.me/MuseTwin_bot?start={user.referral_code}"
                         await bot.send_message(
diff --git a/bot/config.py b/bot/config.py
index 9598e93..959c00e 100644
--- a/bot/config.py
+++ b/bot/config.py
@@ -17,5 +17,6 @@ class Config:
     MINI_APP_URL = os.getenv("MINI_APP_URL", "https://bankahooks.github.io/MuseTwinDate/")
     GIGACHAT_API_KEY = os.getenv("GIGACHAT_API_KEY")
     GIGACHAT_MODEL = os.getenv("GIGACHAT_MODEL", "GigaChat")
+    GIGACHAT_CA_BUNDLE_FILE = os.getenv("GIGACHAT_CA_BUNDLE_FILE")
     VK_ACCESS_TOKEN = os.getenv("VK_ACCESS_TOKEN")
 config = Config()
\ No newline at end of file
diff --git a/bot/docker-compose.yml b/bot/docker-compose.yml
index 14c5236..91852cc 100644
--- a/bot/docker-compose.yml
+++ b/bot/docker-compose.yml
@@ -4,8 +4,8 @@ services:
     build: .
     environment:
       - BOT_TOKEN=${BOT_TOKEN}
-      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/muse_db
-      - REDIS_URL=redis://redis:6379/0
+      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-muse_db}
+      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
       - PAYMENT_PROVIDER_TOKEN=${PAYMENT_PROVIDER_TOKEN}
       - ADMIN_IDS=${ADMIN_IDS}
       - USE_WEBHOOK=False
@@ -19,18 +19,18 @@ services:
   postgres:
     image: postgres:15-alpine
     environment:
-      POSTGRES_USER: postgres
-      POSTGRES_PASSWORD: postgres
-      POSTGRES_DB: muse_db
+      POSTGRES_USER: ${POSTGRES_USER:-postgres}
+      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
+      POSTGRES_DB: ${POSTGRES_DB:-muse_db}
     volumes:
       - pg_data:/var/lib/postgresql/data
-    ports:
-      - "5432:5432"
+    # Не публикуем 5432 на хост — доступ к БД только из внутренней docker-сети.
+    # Если нужен доступ снаружи для отладки, используйте "127.0.0.1:5432:5432".
     restart: unless-stopped
   redis:
     image: redis:7-alpine
-    ports:
-      - "6379:6379"
+    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:?REDIS_PASSWORD must be set}"]
+    # Аналогично не публикуем 6379 наружу.
     restart: unless-stopped
 volumes:
   pg_data:
\ No newline at end of file
diff --git a/bot/handlers/likes.py b/bot/handlers/likes.py
index 22d4924..5106cff 100644
--- a/bot/handlers/likes.py
+++ b/bot/handlers/likes.py
@@ -84,13 +84,20 @@ async def show_likes_for_message(message: Message, state: FSMContext, session: A
 async def like_back_callback(callback: CallbackQuery, state: FSMContext, session: AsyncSession):
     target_id = int(callback.data.split("_")[1])
     user = await crud.get_user_by_telegram_id(session, callback.from_user.id)
+    if not user:
+        await callback.answer("Зарегистрируйтесь через /start")
+        return
     target = await crud.get_user_by_id(session, target_id)
     if not target:
         await callback.answer("Пользователь не найден.")
         return
+    if not await crud.can_like(session, user):
+        await callback.answer("Вы исчерпали лимит лайков на сегодня (30). Купите премиум!", show_alert=True)
+        return
 
     # Создаём лайк (проверка дубликатов внутри)
     like = await crud.create_like(session, user.id, target.id)
+    await crud.increment_likes(session, user)
 
     # Уведомление о количестве лайков для цели (если изменилось)
     total_likes = await get_likes_count(session, target.id)
diff --git a/bot/utils/ai.py b/bot/utils/ai.py
index d110d20..732f1ee 100644
--- a/bot/utils/ai.py
+++ b/bot/utils/ai.py
@@ -12,7 +12,8 @@ logger = logging.getLogger(__name__)
 
 client = GigaChat(
     credentials=config.GIGACHAT_API_KEY,
-    verify_ssl_certs=False,
+    verify_ssl_certs=True,
+    ca_bundle_file=config.GIGACHAT_CA_BUNDLE_FILE,
     model=config.GIGACHAT_MODEL
 )
 
